{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "cd HospitalApp.web/client && npm install && npm run build",
  "outputDirectory": "HospitalApp.web/client/build",
  "framework": "create-react-app"
}
